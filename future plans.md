# Future plans

The only active backlog. Product goal: dependable local prediction and spelling
assistance throughout Linux, with Omarchy first. Tested coverage is in README;
“every app” remains an expansion goal, not a current compatibility claim.

## Next

### Device model discovery and qualification (2026-09-10)

- [x] Inspect this laptop and current primary model/runtime sources; choose a
      short cross-family shortlist based on a concrete continuation hypothesis.
- [x] Integrate bounded Hugging Face discovery, pinned verified downloads and
      model selection into the existing HTTP Prediction Lab.
- [x] Reuse hardware advice for architecture-aware fit gates and identity-bound
      empirical qualification; metadata and speed alone cannot recommend a model.
- [x] Freeze multilingual development and untouched confirmation cases, then
      compare full additions through the Lab and actual writing provider at 550 ms.
- [x] Measure startup, warm reuse, paced input, cancellation and resource pressure;
      retain every failed request and report no qualified model if gates fail.
- [x] Verify the rendered workflow and required source checks; document exact
      candidate identities, results, limitations and reproducible commands.

The [360-request development comparison](evaluation/writing/README.md#measured-shortlist-development-2026-09-10)
and 80 regression requests completed with verified cleanup. Three approximately
350M models used less RAM but none passed English/German/Persian usefulness and
harm gates at 550 ms. The confirmation set remains sealed; no model was promoted.
Measured cold/preparation/reuse/paced/cancellation and 30-second or five-minute
runtime diagnostics are retained separately from quality. Actual Vulkan execution
was verified; reusable qualification remains CPU-only. The rendered HTTP workflow
covers selection, review, explicit receipt saving/rechecking and cancellation.
Final lifecycle fixes invalidate older performance evidence, including canceled
cleanup failures; historical measured-build receipts cannot qualify changed code.
Future promotion still requires a useful new hypothesis, fresh independent cases,
native Persian adjudication where ambiguous, and the full qualification gates.

### Prediction quality and test workspace (2026-09-09)

The user's revised order is research, a usable text/expected-output test part,
then useful contextual predictions and explicit writing-style adaptation; UI
polish follows evidence of useful output. The named first app, “city tour”, is
awaiting clarification. GPT/Codex and Obsidian remain later application targets.

- [x] Review primary research and previous failed ablations; choose exact word
      boundary handling, bounded contextual continuation and explicit style
      controls as separate falsifiable experiments.
- [x] Deliver the local Prediction Lab: editable drafts and expected alternatives,
      actual model prompts/results, reproducible comparison, explicit import/export,
      cancellation and no implicit prose retention. Verify the real local model
      and the rendered user flow.
- [x] Compare fresh development boundary/context/style cases and ordered typing
      traces; preserve language, latency, errors, abstention and first-word
      agreement separately. Review useful alternatives without reference leakage.
- [x] Measure actual prompt reuse and context preparation during paced typing;
      include cancellation, queue delay and cold startup separately. Confirm the
      pinned runtime's zero-token behavior before relying on prompt-only priming.
- [x] Compare the same-family Qwen3-1.7B Base checkpoint through an explicit
      Lab-only artifact path. Freeze identity, matched generation settings and
      full-continuation review; do not infer autocomplete quality from chat scores.
- [x] Isolate instruction prompting from boundary healing, including genuine
      partial words. Compare full continuations at matched diagnostic budgets;
      require a separate typing-speed test before considering promotion.
- [ ] Reduce the combined instruction/boundary mode's measured latency. Count
      preparation cost separately, preserve terminal completion and measure
      useful output within the actual typing deadline before confirmation.
- [x] Rerun source checks and adapt the isolated Omarchy panel/bar tests to the
      reviewed 4.0.3 host. Verify 100 real copied-shell cycles and forced cleanup.
- [x] Restore a reproducible HTTP Lab launch, with an optional fixed port and
      visible launch instructions when the raw HTML is opened without its server.
- [x] Compare explicit Lab-only batches of 16 and 64 on the same prepared
      instruction/boundary inputs, preserving the 18/22 useful-result gate,
      language counts, exact runtime identity and separate cancellation recovery.
- [x] Test a single complete word with an explicit separator and actual runtime
      terminal response. Keep the same instruction prompt, boundary safeguards
      and 550 ms target; use fresh multilingual intent controls before promotion.
- [ ] Prevent facts from supplied writing examples from overriding current
      context. The one-word study delivered Friday despite a Tuesday agreement;
      a well-formed, timely word is insufficient evidence of useful prediction.
      The Lab now abstains with `style_fact_conflict` when a completed suggestion
      introduces a weekday or numeric token that is present in the style examples
      and absent from the current draft and context. This unit fence is not a
      fresh multilingual model confirmation.
- [x] Verify the combined separator/native-stop mechanism on fixed literals.
      All three pairs saved one native generation step with exact output and
      cleanup; this does not qualify natural prediction quality or timing.
- [x] Test German/Persian word completion using exact words from explicitly
      supplied context/style. Preserve spelling, source and caret boundaries;
      contextual reuse does not certify spelling or intended meaning.
- [x] Test model-free completion of a uniquely matching partial word from
      explicitly supplied context/style or earlier authorized draft text. Use a
      separate deterministic identity and test ambiguity, clipped boundaries and
      exact Unicode seams before provider integration.
- [ ] Distinguish an intended partial word from a completed word before enabling
      contextual lookup automatically. Keep the independent 30-case semantic
      controls and the failed quality gate; exact source matching is insufficient.
      Dictionary membership is the wrong splitter for those controls: `mire` is
      in the pinned English lexicon and must still complete to `Mirella`, while
      `marin` is absent and must not complete to `Marinella`.
- [x] Build and evaluate a German/Persian completed-word correction workbench;
      test names, ambiguity, exact preview ranges, cancellation and cleanup.
- [ ] Improve correction usefulness and unwanted-name/nearby-word behavior on
      independent cases before application integration; keep the failed gate.
- [ ] Promote only an evidenced quality improvement into the writing provider,
      validate target application behavior, then improve its UI/UX.

The Lab's real-model browser flow now exercises visible predictions, export,
keyboard import, cancellation, clear during an active model session and recovery.
The fresh prepared one-word comparison failed: neither mode produced a useful
on-time result across 24 main cases, eight per language. The constrained mode
delivered two English additions within 550 ms, one neutral and one wrong date;
German and Persian had no eligible output. Six completed-word controls per mode
had no eligible delivery, which preserves boundaries without establishing coverage.
All 60 target opportunities, including five unavailable targets, remain counted;
115 actual primer/target requests completed with stable artifacts and cleanup.
The mode stays an unchecked Lab experiment; installed defaults are unchanged.
The fixed stop-token probe then measured terminal reductions of 49/95/179 ms
for three forced words, with the Persian case still above 550 ms. Applying that
mechanism to natural generation is the next latency experiment; it must retain
current-context facts and language-specific quality gates before promotion.
The matched 48-request development run completed with stable identities and
cleanup: full-context healing yielded 10/16 substantive first words in an
independent blinded agent review, versus 3/16 for current provider logic. This
small synthetic result motivates an isolated production-logic boundary mode;
it does not establish natural writing quality. All nine Persian requests in
the separate ordered trace run produced no suggestion within 550 ms. A fresh
120-case confirmation completed with 240 valid execution/cleanup records. Its
blinded review found 2/60 substantive first words for baseline trailing-space
cases versus 6/60 for the isolated fix: the +4 gain fails the predeclared +8 gate.
Unhelpful outputs fell from 24/60 to 5/60, but the fix remains experimental.
All 60 no-space control payload pairs match; three German outputs differ in
length around the deadline. The result does not establish useful contextual or
personalized predictions.

The Lab now fences uploads started before reset, verifies runtime ownership and
executable identity before readiness, and contains startup parent death. Rust
process regressions, delayed-upload server tests and the aggregate checks pass.
An actual model worker killed before readiness also lost its owned runtime in
5 ms; a rebuilt browser trial verified visible output and clear during an active
session. The normal installed runtime was preserved.

The Lab is an opt-in development surface and does not enable the quarantined
native browser/Codex editing path or qualify application coverage. The earlier
132-case heldout and 120-case confirmation sets are exposed development evidence.
The next development suite is frozen in
`output/writing/2026-09-09-context-traces-frozen/`: 12 independent contextual
traces, four per language, with four snapshots and exactly one Unicode scalar
added at each transition. Half include explicit style examples. Its 0/100/250/500
ms schedule has now run in 24 independent cold/primed trials, preserving all 96
opportunities. Cold inference produced no eligible final output; priming produced
three, of which blinded full-continuation review found one useful and two
factually conflicting. This fails both the +3 usefulness gain and non-increasing
harm gates. All four Persian priming attempts exceeded the 1,500 ms prelude.
The paced-only transient half-space rule retains the typing revision without
issuing a model request; production text guards remain unchanged.

The same-family Base comparison completed 32 fresh-runtime requests with all
16 paired prompts/payloads identical. Blinded full-continuation review found
8/16 useful eligible outputs for Base versus 6/16 for the installed artifact.
The +2 gain fails the frozen +3 gate, and Persian useful yield fell from 1/4 to
0/4. Neither artifact is promoted. This exposed, small development set contains
related boundary pairs; the quantized artifacts also differ in their embedding
and output tensors, so it does not isolate pretraining from post-training.

The matched instruction/boundary diagnostic completed 66 requests on 22 cases:
blinded full-continuation review found 18 useful combined-mode outputs, versus
13 with healing alone and six with instructions alone. It passes the frozen
development gain, language and harm gates. However, combined-mode cold median
latency was 2,809.5 ms, with no eligible terminal result within 550 ms. All four
combined-mode abstentions are German/Persian midword guard rejections. The
candidate remains Lab-only pending latency work and independent confirmation.
The successful run is an unchanged retry; an earlier attempt ended by SIGTERM
after 65/66 records and retains its incomplete receipts, without a known cause.

Preparing the combined instructions before each target produced 11 useful
on-time outputs out of 22, versus none cold, plus one neutral and no harmful
outputs in blinded agent review. This fails the frozen 18-useful target.
Preparation itself took a median 1,404.9 ms, excluding runtime startup; this
experiment did not replay typing. The candidate remains Lab-only. The separate
German/Persian Spelling tab now provides explicit tests and a grey correction
preview. The real 40-case run completed with verified cleanup but failed quality:
two useful German and four useful Persian reference matches, with one unwanted
control change in each language. All eight offers arrived within 126 ms over
HTTP in that run; speed does not compensate for incorrect corrections. The
initial startup failure and subsequent Btrfs verification fix remain recorded.

The separate Word completion tab now performs exact model-free lookup with grey
previews, explicit context/style, local expected-answer scoring and clear/retry.
Its frozen HTTP30 test produced all 12 intended completions and abstained on
15/18 controls, but extended a completed word incorrectly in each language:
`Marin` to `Marinella`, `Ring` to `Ringordner`, and `سرما` to `سرمایه‌گذاری`.
The quality gate failed. All responses arrived within 35.27 ms over HTTP,
including process verification and cleanup; that timing is not visible latency.
All 30 one-shot workers exited cleanly without a model or dictionary. The actual
browser verified English/German/Persian previews, ambiguity withdrawal, tab
navigation and clear/retry. This remains an explicit test, not an installed
prediction improvement.

The context-word recovery completed 24 requests with 12 identical paired model
payloads and raw outputs. It recovered two useful German words versus zero for
the control, below the frozen +3 gate; neither arm produced useful Persian
output. Both retained two harmful repeated non-Persian suggestions from the
legacy path. The two recovered words have valid exact source boundaries; none
of the negative controls falsely authorized recovery. No result met 550 ms.
Separately, a frozen 48-word native Hunspell probe found the reference among
suggestions for all six authored typos in each language, but Persian ranked it
first only three times. Names, partial words and ambiguity require additional
guards; dictionary results are not a production correction policy.

### Active recovery and useful-writing goal (2026-09-07)

The initial failure was **no suggestions**, across mixed applications and
languages. The user rejected the recovered experience: prediction quality is
poor, native suggestions need a readable grey/translucent preview, correction
must work, and browser and Codex desktop support must require no extension or
per-application plugin. Terminal support is also required. The plumbing repairs
below are a baseline, not completion of the product goal.

- [ ] Improve context-aware completion and conservative word correction using
      development-only model comparisons; freeze a new independent confirmation
      set before claiming writing quality. The earlier held-out set is exposed.
- [x] Render real grey inline suggestions in Bash/Readline, with safe clearing,
      explicit acceptance and native undo. Distinguish the shell buffer from
      arbitrary terminal applications.
- [ ] Prove extension-free field identity, context, caret preview, insertion,
      correction and undo in Chromium and the current Codex desktop composer.
      Investigate AT-SPI plus cooperative Fcitx input; preserve per-site policy,
      password/composition denial, and exact focused-field agreement.
- [ ] Replace the native candidate-only experience with a readable caret preview
      that preserves focus and yields to foreign input-method composition.

Source progress: the broker, native addon, accessibility helper and grey Bash
preview are installed with backups. Physical Ghostty 1.3.1 / Bash 5.3.15 /
Readline 8.3 trials passed real-model preview, explicit acceptance, conservative
English correction and exact native undo; the command buffer stayed unchanged
during preview and nothing was submitted. Ten PTY tests cover rendering, signals,
Persian offsets and native undo. The browser-trial startup changes were later
withdrawn: original Chromium flags are restored and task-created Codex flags are
removed; a normal relaunch cannot enable the quarantined native editing path.

The extension-free observer and actual Fcitx addon pass private socket/D-Bus
checks for exact origin policy, automatic requests, stale fields, pause and
restart. The installed proxy-disposal fix removes an observation reset loop;
previews bind absolute caret and document length before rendering. The opt-in,
version-pinned Fcitx frontend passes 13 private-compositor scenarios and resolved
Chromium v3's stalled publication handshake during the installed trial. After
editing failed, the normal desktop returned to the stock frontend; the patch and
receipts remain research artifacts. The separate v1 commit_state mismatch remains
unsupported.

A sandbox-enabled Chromium 151 trial shows grey real-model text and accepts it,
but native undo also removes the preceding typed prefix. Both native correction
and single-call append failed exact-field authority: page handlers redirected
text into another field or moved insertion to the start of the original field.
Cancellation and post-insertion focus controls distinguish the failure from
ordinary page focus changes. Native replacement is withdrawn, and the native
browser/Codex path is quarantined. Do not implement completion undo with hidden
suffix deletion, fake preedit or an ambiguous navigation key. Evidence is in
`output/extensionless/installed-browser-03/` and `installed-browser-04/`.
The next extension-free prerequisite is an app-owned transaction channel or
negotiated upstream field/revision authority through renderer execution. A
Chromium reentrancy fix alone cannot bind a queued edit to Badi's earlier snapshot.
Codex composer, Brave geometry, and the remaining physical matrix stay open.
The installed quarantine passed a sandbox-enabled Chromium check with an explicit
origin allow: original Tab navigation and text were preserved, with no model
context or commit. The installed notice-lifetime fix also passed a later physical
stock-frontend trial: explicit invocation displayed the unavailable notice,
while text and Tab navigation were preserved. The auxiliary panel's position
does not qualify caret alignment. Evidence: `installed-browser-05/` under the
same output directory; all disposable browser origins and debug sessions were
removed afterward.

The installed writing client now applies its 550 ms budget while waiting for
HTTP headers as well as streaming. Exhaustion returns no proposal and closes the
request; a correction cannot enqueue continuation after spending that budget.
Cancellation, actual HTTP/transport errors and the historical semantic deadline
keep their existing meaning. This fixes deadline classification, not upstream
cancellation speed or prediction usefulness. Required Rust/MSRV and aggregate
source checks pass. A fresh immediate-recovery 12-request owned-runtime check
confirmed cancelled calls end promptly, later requests recover, and Persian
header-budget exhaustion at 551 ms returns no output/error; both children exited
0. That result does not establish faster upstream cancellation.

The frozen 240-request context/word-limit experiment also rejected default
promotion. Substantive first-word yields were 18/14/13/16 per 60 requests for
sentence/four words, sentence/one word, 20-token/four words and 20-token/one word.
All four arms failed the substantive prior-sentence cue cases. Neutral function
words are counted separately, and fixed order/runtime speed variation limits
latency attribution. The blinded review and measurements are in
[writing evaluation](evaluation/writing/README.md); this is no quality or parity
achievement.

The new independently authored 120-case confirmation and blinded agent review (with prior project involvement)
rejected the longer-context writing change: baseline 53 shown /10 useful first
words versus candidate 8 shown /3 useful first words. No errors occurred, but
cold context prefill exceeded the 550 ms budget. The prior sentence
clipping is restored while retaining correction; this restoration is not a
quality improvement over the baseline. The baseline is also weak on this set. New small base-model
experiments and natural writing benefit remain open.

1. [x] Restore dependable startup using a verified installed model that fits
       available resources. Reproduce the missing-model failure, test resource
       changes, expose actionable offline diagnostics, and verify restart.
2. [x] Recover adapters after broker restart without reopening documents.
       Prove fresh policy/context binding, cancellation, acceptance and undo.
3. [x] Deliver automatic suggestions and next-word acceptance on supported
       editor-owned fields. Preserve explicit manual invocation where native
       widget authority is unknown; document exact app and language boundaries.
4. [ ] Freeze development and held-out multilingual writing samples before
       tuning. Evaluate at least 100 held-out prefixes, usefulness and abstention
       separately, error counts, useful words and keystroke savings. Measure
       broker and visible latency separately; aim for warm visible p50 <=250 ms
       and p95 <=500 ms, and report misses rather than weakening the metric.
5. [ ] Validate the installed user flow with disposable documents, service
       restart, native undo, pause, composition and stale-focus checks. Require
       zero wrong-field or stale edits in the exercised matrix and all affected
       repository checks passing. Review results independently before delivery.

The current service failure was `start-limit-hit`: hardware advice selected an
uninstalled Qwen3-0.6B model despite the existing Qwen3-1.7B installation. A
2026-09-07 restart with sufficient current resources restored the old binary in
4.03 seconds. The installed fix selects a verified pinned model already present
within hard resource limits. Broker, native addon, editor files and Omarchy panel
are updated; full installation verified the mapped addon and preserved the
keyboard profile, app/site policy, pause and autostart settings.

The actual broker and native host now recover from owned-model exit and socket
EOF. Browser retries retain backoff through a cold restart. Real Obsidian and
Chromium local-model checks passed open-document recovery, fresh acceptance and
undo. Installed native Fcitx routing passed both app identities and denied-field
checks; an Omawrite 0.5.0 / Qt 6.11.2 desktop trial verified visible prediction,
saved insertion and exact native undo. The broader native composition/app matrix
still needs current physical trials.
Xournal++ 1.3.7 also passed visible prediction, saved text-object mutation and
exact document undo after leaving text-edit mode.

The frozen 132-prefix comparison produced 94 suggestions and zero errors versus
38 suggestions and 13 errors. Provisional useful-next-word counts were 63/94
versus 24/38. Candidate review was not independent because the support agents
reached their usage limit. Broker suggestion p50/p95 rose to 346/553 ms. The
final 16-sample visible Chromium run measured 348/589 ms, missing both targets;
a concurrent-build run missed a suggestion. Persian coverage remains 12/44.
Full evidence and review limitations are in `evaluation/writing/README.md`.
Continue with an independent review, a new confirmation set before further model
tuning, Persian quality/latency, sustained-load reliability, and real writing
acceptance/keystroke measurements. Items 4 and 5 remain open for those gates.

Cotypist feature claims
guide the work, but parity requires the same tasks on a named Cotypist version
and supported Mac. Source research cannot establish comparative performance.

### Coverage and quality expansion

- [ ] Extend native coverage beyond Omawrite/Xournal++ through measured toolkit
      integrations; add automatic requests where IME cooperation is reliable.
- [ ] Add browser contenteditable, rich editors, frames/shadow roots and Firefox.
      Preserve exact field binding, native undo and per-site consent.
- [ ] Support Fish/Zsh and selected terminal editors through their own editable
      buffers. Never accept by executing a generated command.
- [ ] Native/Obsidian/general-web spelling correction with exact replacement
      ranges, conservative precision and explicit acceptance. Multilingual
      prediction needs a separate evaluation corpus and language controls.
      German/Persian correction needs full dictionary semantics and explicit
      Unicode replacement counts; see the dated feasibility note in
      [Linux architecture research](docs/research/linux-architecture.md).
- [ ] Complete the active evaluation's independent review and observed writing
      benefit; evaluate spelling replacement separately from continuation.
      Keep useful yield, abstention, errors and visible latency separate.
- [ ] Expand composition, focus, navigation, zoom, accessibility and restart
      checks on real application versions. Preserve a tested compatibility matrix.
- [ ] Unify adapter activity and actionable missing-integration/model feedback;
      add shortcut/debounce settings without storing typed prose by default.
- [ ] Verify installation, model provisioning, update/reload and uninstall on a
      fresh Omarchy session, with reversible user-local changes and health checks.
- [ ] Measure CPU/GPU and lower-memory hardware: cold start, RAM, sustained
      latency, power, cancellation and runtime crash recovery.

## Later

- [ ] Other Linux desktops and a standard tray integration.
- [ ] Opt-in personalization with explicit retention/clear controls.
- [ ] Source-bound release receipts, packaging, licensing and naming review.
      No publication or Git history changes without the user's request.

Read `AGENTS.md` and the relevant source/runbook, not the research corpus by
default. Update this file in place. Keep comments for non-obvious invariants and
complex decisions. Completed context: [what has been built](what-have-been.md).
